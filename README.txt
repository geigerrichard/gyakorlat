HUN
- az adatbázist importálni kell az első használat előtt, az exportált verzió az app/db mappában megtalálható (gyakorlat.sql)
- importálást követően az app/config/config.php-ban megváltoztatandó a DB-hez tartozó konstansok értéke

ENG
- the database must be imported before the first use, the exported version in the app/db folder (gyakorlat.sql)
- after the import, you should change the constants values at the app/config/config.php file, if it is necessary and the values does not match with your phpmyadmin settings
